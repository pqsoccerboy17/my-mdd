---
sidebar_position: 2
title: Changelog
sidebar_label: Changelog
hide_table_of_contents: true
---

import Timeline from '@site/src/components/Timeline';
import { eras as historicalEras, computeProjectStats } from '@site/src/components/Timeline/changelogData';
import syncData from '@site/src/data/changelog-sync.json';

export const allEras = [...historicalEras, ...(syncData.eras || [])].reverse();
export const stats = computeProjectStats(allEras);

MDD HQ has been built from scratch starting February 22, 2026. This is the full story -- from an empty directory to a production dashboard and beyond, updated daily.

<Timeline data={allEras} />

*Changelog is automatically synced from MDD HQ development activity.*
